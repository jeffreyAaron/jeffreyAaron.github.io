---
# This title is used for the browser tab and the link-preview card (see the
# "description" and "thumbnail" keys below). The title displayed at the top of
# the page is set separately, on the <Header> component in the body.
title: CS 180 Project 0

# The color theme of the page. Defaults to "device" (the preference set in the user's browser or operating system). Setting this to "light" or "dark" will override the user's preference. This is useful if your figures only look good in one theme.
theme: light

# This is the icon that appears in the user's browser tab. To customize, change the favicon.svg file in /public/, or add your own file to /public/ and change the filename here.
favicon: favicon.svg

# These keys are optional. If a link to your project page is in a Google search result, text message, or social media post, it will often appear as a "link preview card" based on its title, description, favicon, and thumbnail.
description: CS 180 Project 0 — Becoming Friends with Your Camera
thumbnail: screenshot-light.png
---

import Header from "./components/Header.astro";
import HighlightedSection from "./components/HighlightedSection.astro";
import Figure from "./components/Figure.astro";
import Picture from "./components/Picture.astro";
import TwoColumns from "./components/TwoColumns.astro";

import dogsDiffc from "./assets/dogs-diffc.png";
import dogsTrue from "./assets/dogs-true.png";
import selfieClose from "./assets/part1-selfie-close.jpg";
import selfieFar from "./assets/part1-selfie-far.jpg";

{/*
  Assignment: https://cal-cs180.github.io/fa26/hw/proj0/index.html (due Tue Sep 1, 2026, 11:59 PM)

  Everything below is a placeholder — swap dogsDiffc/dogsTrue for your own photos in
  src/assets/, and fill in the explanations. The three sections below map directly to the
  three required parts of the assignment; don't remove any of them.

  Duplicating this project for a new assignment instead? Copy this whole cs180-src/proj0
  folder, then update:
    - astro.config.ts: base (bump "proj0" to "proj1", etc.)
    - the title/description below and the <Header> props
    - swap the images for your own in src/assets/
*/}

<Header
  title="Project 0: Becoming Friends with Your Camera"
  conference="CS 180, UC Berkeley — Fall 2026"
  authors={[{ name: "Jeffrey Aaron", url: "https://jeffreyaaron.dev", institution: "UC Berkeley" }]}
  links={[
    { name: "CS 180 Projects", url: "../", icon: "ri:arrow-left-line" },
    { name: "Code", url: "https://github.com/jeffreyAaron", icon: "ri:github-line" },
  ]}
/>

<Picture src={dogsTrue} alt="Lead photo for the project (replace with your own)" priority />

<HighlightedSection>

## Overview

This project is about building intuition for how perspective, focal length, zoom, and center of
projection affect a photo — through three hands-on shooting exercises.

</HighlightedSection>

## Part 1: Selfie comparison

A close-up selfie versus a portrait taken from farther away with zoom, keeping the face roughly
the same size in frame.

<TwoColumns>
  <Picture slot="left" src={selfieClose} alt="Close-up selfie-style portrait" />
  <Picture slot="right" src={selfieFar} alt="Portrait taken from farther away, zoomed in" />
</TwoColumns>

Explain why the zoomed-in, farther-away shot looks better — what's happening to facial proportions
as the camera gets closer.

## Part 2: Architectural perspective

The same building photographed twice: once zoomed in from far away, once up close without zoom,
keeping the building roughly the same size in frame both times.

<TwoColumns>
  <Picture slot="left" src={dogsDiffc} alt="Building photographed with zoom from far away" />
  <Picture slot="right" src={dogsTrue} alt="Building photographed up close without zoom" />
</TwoColumns>

Explain the compression/flattening effect — how the perceived depth and converging lines of the
building change between the two shots.

## Part 3: Dolly zoom

A sequence of stills taken while walking backward and zooming in simultaneously, keeping the
subject roughly the same size throughout, combined into an animated GIF.

<Figure>
  <Picture slot="figure" src={dogsTrue} alt="Dolly zoom sequence (replace with your GIF)" />
  <Fragment slot="caption">Dolly zoom sequence — replace this with your combined GIF.</Fragment>
</Figure>

Describe what's happening to the background relative to the subject as you dolly out and zoom in.
